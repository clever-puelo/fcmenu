"""Capa de interfaz gráfica (PyQt6) de FCMENU.

Se migra formulario por formulario desde los `.frm` de VB6, siguiendo el
flujo sugerido en `CLAUDE.md`. Cada módulo de este paquete documenta en su
docstring de qué `.frm` legacy proviene y qué diferencias de comportamiento
tiene (todas confirmadas con el usuario antes de codear).
"""
